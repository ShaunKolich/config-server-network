spring:
  application:
    name: profile
    
  datasource:
    url: jdbc:mysql://localhost:3306/profile
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
   port: 9001
